import * as cdk from 'aws-cdk-lib';
import { Construct } from 'constructs';

export class LendPeak2Stack extends cdk.Stack {
  constructor(scope: Construct, id: string, props?: cdk.StackProps) {
    super(scope, id, props);

    // This is a placeholder stack that can be used for shared resources
    // or cross-stack references in the future
  }
}